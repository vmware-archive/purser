apiVersion: apps/v1
kind: Deployment
metadata:
  name: kuber-controller
spec:
  replicas: 1
  template:
    metadata:
      labels:
        app: kuber
    spec:
      containers:
      - name: kuber-controller
        image: kuber-controller:1.0.0
        ports:
        - containerPort: 80